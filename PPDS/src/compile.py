import os


# these return the name of an .so-file to use with ctypes
# where to put the generated python-file?
# -> in the folder (may be temporary) where the thing is compiled, in a python_generated folder, just like the headers

def from_string(s):
    pass

def from_directory(dir):
    # look for makefile and execute it
    pass